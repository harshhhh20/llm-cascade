# Copy this file to .env (same directory as docker-compose.yml) and fill in
# your real key. docker compose automatically loads .env — no need to export
# it in your shell or pass it via `docker exec -e` to the wrong container.
FRONTIER_API_KEY=your-key-here
